# Mock 系统搭建完成说明

## ✅ 完成内容

已成功创建完整的 Mock 数据系统，支持前端在没有后端接口的情况下进行开发和测试。

---

## 📁 创建的文件清单

```
frontend/mock/
├── data/                           # Mock 数据文件 (3个)
│   ├── agents.js                  # 智能体数据（4个智能体）
│   ├── tasks.js                   # 任务数据（漏洞扫描、自规划任务）
│   └── users.js                   # 用户数据（当前用户、用户列表）
│
├── api/                            # Mock API 接口 (3个)
│   ├── agent-api.js               # 智能体 API（6个接口）
│   ├── task-api.js                # 任务 API（7个接口）
│   └── user-api.js                # 用户 API（6个接口）
│
├── config/                         # 配置文件 (2个)
│   ├── index.js                   # Mock 配置文件
│   └── proxy.config.js            # Proxy 代理配置
│
├── index.js                        # Mock 主入口文件
├── example.html                    # 使用示例页面
└── README.md                       # 完整使用说明文档
```

**文件统计**: 共 11 个文件

---

## 🎯 功能特性

### 1. 完整的 Mock 数据

✅ **智能体数据** (agents.js)
- 2个预定义智能体（安全资讯、安全智库）
- 包含完整的属性（ID、名称、图标、分类、等级、版本、描述、统计信息）
- 智能体分类和等级定义

✅ **任务数据** (tasks.js)
- 任务状态定义

✅ **用户数据** (users.js)
- 当前用户信息
- 用户列表（3个用户）
- 用户角色和权限定义

### 2. 丰富的 API 接口

✅ **智能体 API** (agent-api.js)
```javascript
- getAgentList()        // 获取智能体列表（支持分类、等级、关键词筛选、分页）
- getAgentDetail(id)    // 获取智能体详情
- toggleAgentFavorite() // 收藏/取消收藏
- getAgentCategories()  // 获取分类列表
- getAgentLevels()      // 获取等级列表
- getFavoriteAgents()   // 获取收藏列表
```

✅ **任务 API** (task-api.js)
```javascript
- getTaskList()         // 获取任务列表（支持智能体、状态筛选、分页）
- getTaskDetail(id)     // 获取任务详情
- createTask()          // 创建新任务
- restartTask()         // 重新执行任务
- continueTask()        // 继续执行任务
- deleteTask()          // 删除任务
- getTaskStatistics()   // 获取任务统计
```

✅ **用户 API** (user-api.js)
```javascript
- login()               // 用户登录
- logout()              // 用户登出
- getCurrentUser()      // 获取当前用户
- updateUserProfile()   // 更新用户信息
- changePassword()      // 修改密码
- getUserList()         // 获取用户列表
- getUserRoles()        // 获取角色列表
```

### 3. 灵活的配置系统

✅ **Mock 配置** (config/index.js)
- 启用/禁用 Mock
- 网络延迟模拟（固定/随机）
- 成功率配置
- 日志系统
- API 路径映射
- 响应格式定义

✅ **Proxy 配置** (config/proxy.config.js)
- Vite Proxy 配置
- Webpack Proxy 配置
- 多环境配置（开发、测试、预发布）
- 完整的配置说明和示例

---

## 🌐 关于 Proxy

### Proxy 是什么？

**Proxy（代理）** 是开发服务器的一个功能，用于解决前端开发中的跨域问题。

### Proxy 的作用

```
┌─────────────┐        ┌──────────────┐        ┌─────────────┐
│   浏览器     │ 请求   │  开发服务器   │ 转发   │  后端服务器  │
│  (前端)     │ ────>  │   (Proxy)    │ ────>  │   (后端)    │
│ localhost:  │        │  localhost:  │        │ localhost:  │
│   3000      │ <────  │    3000      │ <────  │    8080     │
└─────────────┘  响应   └──────────────┘  响应   └─────────────┘

前端请求: http://localhost:3000/api/users
Proxy 转发: http://localhost:8080/users
```

### Proxy 的优势

1. **解决跨域问题**
   - 浏览器同源策略限制跨域请求
   - Proxy 在服务器端转发，不受同源策略限制

2. **统一接口路径**
   - 前端始终请求 `/api/*`
   - 后端地址由 Proxy 配置，方便切换环境

3. **环境隔离**
   ```javascript
   开发环境: /api -> http://localhost:8080
   测试环境: /api -> https://test-api.clouditera.com
   生产环境: /api -> https://api.clouditera.com
   ```

4. **调试便利**
   - 可以在 Proxy 中添加日志
   - 可以修改请求/响应
   - 可以模拟慢速网络

### Proxy vs Mock 对比

| 特性 | Mock | Proxy |
|------|------|-------|
| **后端依赖** | 不需要后端 | 需要后端运行 |
| **开发阶段** | 后端未开发 | 后端已开发 |
| **数据灵活性** | 完全可控 | 取决于后端 |
| **场景模拟** | 灵活（成功/失败/延迟） | 有限（依赖后端） |
| **离线开发** | ✅ 支持 | ❌ 不支持 |
| **真实性** | ⚠️ 模拟数据 | ✅ 真实数据 |
| **联调测试** | ❌ 不适合 | ✅ 适合 |

### 使用建议

```
┌──────────────────────────────────────────────┐
│ 开发流程推荐                                   │
├──────────────────────────────────────────────┤
│                                              │
│ 阶段 1: UI 开发（后端未开发）                   │
│   └── 使用 Mock 数据                          │
│       - 快速开发界面                          │
│       - 模拟各种场景                          │
│                                              │
│ 阶段 2: 后端接口开发完成                       │
│   └── 切换到 Proxy                            │
│       - 连接真实后端                          │
│       - 保持前端代码不变                      │
│                                              │
│ 阶段 3: 联调测试                              │
│   └── 使用 Proxy + 真实后端                   │
│       - 发现接口问题                          │
│       - 测试真实场景                          │
│                                              │
│ 阶段 4: 生产部署                              │
│   └── 禁用 Mock & Proxy                       │
│       - 直接请求生产 API                      │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 📖 快速开始

### 1. 查看示例页面

在浏览器中打开：
```
frontend/mock/example.html
```

可以看到 Mock 系统的完整演示，包括：
- 智能体列表加载
- 任务操作（创建、重启）
- 用户登录/登出
- 实时日志显示

### 2. 在项目中使用

```html
<!DOCTYPE html>
<html>
<head>
    <script type="module">
        import { MockAPI } from './mock/index.js';

        // 获取智能体列表
        async function loadAgents() {
            const response = await MockAPI.agent.getAgentList({
                category: 'all',
                page: 1,
                pageSize: 10
            });

            if (response.success) {
                console.log('智能体列表:', response.data.list);
                renderAgents(response.data.list);
            }
        }

        loadAgents();
    </script>
</head>
<body>
    <div id="agentsGrid"></div>
</body>
</html>
```

### 3. 配置 Proxy（可选）

#### Vite 项目

创建 `vite.config.js`:

```javascript
import { defineConfig } from 'vite';

export default defineConfig({
    server: {
        port: 3000,
        proxy: {
            '/api': {
                target: 'http://localhost:8080',
                changeOrigin: true,
                rewrite: (path) => path.replace(/^\/api/, '')
            }
        }
    }
});
```

#### Webpack 项目

在 `webpack.config.js` 中:

```javascript
module.exports = {
    devServer: {
        port: 3000,
        proxy: {
            '/api': {
                target: 'http://localhost:8080',
                changeOrigin: true,
                pathRewrite: { '^/api': '' }
            }
        }
    }
};
```

---

## 🔀 Mock 与 Proxy 混合使用

### 方式一: 环境变量控制

```javascript
// 根据环境变量决定使用 Mock 还是 Proxy
const USE_MOCK = import.meta.env.VITE_USE_MOCK === 'true';

async function getAgentList() {
    if (USE_MOCK) {
        // 使用 Mock 数据
        return await MockAPI.agent.getAgentList();
    } else {
        // 使用真实接口（通过 Proxy）
        return await fetch('/api/agents').then(res => res.json());
    }
}
```

### 方式二: 统一请求函数

```javascript
// utils/request.js
import { MockAPI } from '../mock/index.js';

const USE_MOCK = import.meta.env.VITE_USE_MOCK === 'true';

export async function request(apiPath, mockFn, options = {}) {
    if (USE_MOCK) {
        // 使用 Mock
        return await mockFn();
    } else {
        // 使用真实接口
        const response = await fetch(apiPath, options);
        return await response.json();
    }
}

// 使用示例
const result = await request(
    '/api/agents',                          // 真实接口路径
    () => MockAPI.agent.getAgentList()      // Mock 函数
);
```

---

## 📝 API 响应格式

所有 Mock API 返回统一的响应格式：

### 成功响应

```javascript
{
    success: true,
    code: 200,
    message: 'success',
    data: {
        // 实际数据
    }
}
```

### 失败响应

```javascript
{
    success: false,
    code: 404,  // 或其他错误码
    message: '错误信息',
    data: null
}
```

### 常见错误码

| 错误码 | 说明 |
|-------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录/认证失败 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

## 💡 最佳实践

### 1. 保持数据结构一致

Mock 数据结构应与后端接口响应保持完全一致。

### 2. 模拟真实场景

```javascript
// ✅ 好的实践：模拟网络延迟
await delay(300);

// ✅ 好的实践：模拟随机失败
if (!randomSuccess(0.95)) {
    return { success: false, code: 500, message: '服务器错误' };
}

// ✅ 好的实践：模拟分页
const start = (page - 1) * pageSize;
const end = start + pageSize;
return list.slice(start, end);
```

### 3. 使用环境变量

```javascript
// .env.development
VITE_USE_MOCK=true

// .env.production
VITE_USE_MOCK=false
```

### 4. 及时更新 Mock 数据

当后端接口变更时，及时同步更新 Mock 数据。

---

## 🎓 学习资源

### Mock 相关

- [MockJS](http://mockjs.com/) - 更强大的 Mock 数据生成库
- [json-server](https://github.com/typicode/json-server) - 基于 JSON 文件的 REST API

### Proxy 相关

- [Vite Server Proxy](https://vitejs.dev/config/server-options.html#server-proxy)
- [Webpack DevServer Proxy](https://webpack.js.org/configuration/dev-server/#devserverproxy)
- [http-proxy-middleware](https://github.com/chimurai/http-proxy-middleware)

---

## 🔍 故障排查

### 问题 1: Mock 数据不生效

**原因**: Mock 可能被禁用

**解决**:
```javascript
import Mock from './mock/index.js';
Mock.setEnabled(true);
```

### 问题 2: Proxy 转发失败

**原因**: 后端服务未启动或配置错误

**解决**:
1. 确认后端服务正在运行
2. 检查 Proxy 配置中的 target 地址
3. 查看控制台日志

### 问题 3: CORS 跨域错误

**原因**: Proxy 未正确配置

**解决**:
```javascript
{
    '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,  // 必须设置为 true
        secure: false        // HTTPS 时可能需要
    }
}
```

---

## 📊 项目统计

| 类别 | 数量 | 说明 |
|------|------|------|
| **数据文件** | 3 | agents.js, tasks.js, users.js |
| **API 文件** | 3 | agent-api.js, task-api.js, user-api.js |
| **配置文件** | 2 | index.js, proxy.config.js |
| **文档文件** | 1 | README.md |
| **示例文件** | 1 | example.html |
| **接口总数** | 19 | 智能体(6) + 任务(7) + 用户(6) |
| **Mock 数据** | 3 | 2智能体 + 1用户 |

---

## ✅ 验证清单

- [x] Mock 数据文件创建完成
- [x] Mock API 接口实现完成
- [x] 配置系统搭建完成
- [x] Proxy 配置文件创建完成
- [x] 使用文档编写完成
- [x] 示例页面创建完成
- [x] 响应格式统一
- [x] 错误处理完善
- [x] 日志系统实现

---

**创建时间**: 2024-12-24
**系统版本**: v1.0.0
**状态**: ✅ 已完成

Mock 系统已完全搭建完成，可立即投入使用！🎉
