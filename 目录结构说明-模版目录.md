# 目录结构说明 - 模版目录

## 📁 模版目录结构

UI模版库的相关页面已独立到 `frontend/templates/` 目录，结构与 `frontend/agents/` 类似：

```
frontend/
├── templates/                        # UI模版目录
│   ├── pages/                       # 模版HTML页面
│   │   ├── agent-detail-empty.html          # 快速对话模式模版
│   │   ├── agent-detail-search.html         # 信息查看/检索模版（列表页）
│   │   └── agent-detail-search-detail.html   # 信息查看/检索模版（详情页）
│   │
│   ├── css/                         # 模版专用样式
│   │   ├── agent-detail.css         # 智能体详情页通用样式
│   │   ├── agent-search.css         # 信息检索页面样式
│   │   └── agent-news-detail.css     # 信息详情页样式
│   │
│   ├── js/                          # 模版专用脚本
│   │   ├── agent-detail.js          # 智能体详情页通用脚本
│   │   ├── agent-search.js          # 信息检索页面脚本
│   │   └── agent-news-detail.js     # 信息详情页脚本
│   │
│   └── (markdown文档)               # 模版设计文档
│       ├── quick-chat-agent-template.md
│       └── info-search-agent-template.md
│
├── agents/                          # 智能体实际应用目录
│   ├── pages/                       # 实际应用HTML页面
│   ├── css/                         # 实际应用样式
│   └── js/                          # 实际应用脚本
│
└── templates.html                   # UI模版库入口页面
```

---

## 🔗 路径引用规则

### 从 `templates/pages/` 目录引用资源

**引用全局资源（frontend根目录）：**
- CSS: `../../css/common.css`
- JS: `../../js/dashboard.js`
- HTML: `../../dashboard.html`, `../../templates.html`

**引用templates目录下的资源：**
- CSS: `../css/agent-detail.css`
- JS: `../js/agent-search.js`

**引用同目录下的页面：**
- HTML: `agent-detail-search-detail.html`（相对路径，无需前缀）

**引用agents目录下的实际应用页面：**
- HTML: `../../agents/pages/agent-detail-chat.html`

---

## 📋 模版页面列表

### 1. 快速对话模式
- **文件**: `templates/pages/agent-detail-empty.html`
- **样式**: `templates/css/agent-detail.css`
- **脚本**: `templates/js/agent-detail.js`
- **用途**: UI模版库预览 - 展示快速对话模式的界面结构

### 2. 信息查看/检索模式（列表页）
- **文件**: `templates/pages/agent-detail-search.html`
- **样式**: `templates/css/agent-detail.css`, `templates/css/agent-search.css`
- **脚本**: `templates/js/agent-search.js`
- **用途**: UI模版库预览 - 展示信息检索列表页的界面结构

### 3. 信息查看/检索模式（详情页）
- **文件**: `templates/pages/agent-detail-search-detail.html`
- **样式**: `templates/css/agent-detail.css`, `templates/css/agent-news-detail.css`
- **脚本**: `templates/js/agent-news-detail.js`
- **用途**: UI模版库预览 - 展示信息检索详情页的界面结构

---

## 🔄 模版 vs 实际应用

### 模版页面（templates/pages/）
- **位置**: `frontend/templates/pages/`
- **特点**: 通用、脱敏、展示界面结构
- **用途**: UI模版库预览
- **入口**: `templates.html` 中的"预览模版"按钮

### 实际应用页面（agents/pages/）
- **位置**: `frontend/agents/pages/`
- **特点**: 包含具体业务内容和数据
- **用途**: 实际运行的智能体功能
- **入口**: `dashboard.html` 中的"立即使用"按钮

---

## 📝 维护指南

### 添加新模版页面

1. 在 `frontend/templates/pages/` 创建HTML文件
2. 在 `frontend/templates/css/` 创建对应的CSS文件（或复用现有样式）
3. 在 `frontend/templates/js/` 创建对应的JS文件（或复用现有脚本）
4. 在 `templates.html` 中添加模版卡片和预览链接
5. 确保所有路径引用正确

### 路径引用检查清单

- [ ] CSS路径：`../../css/`（全局）或 `../css/`（templates目录）
- [ ] JS路径：`../../js/`（全局）或 `../js/`（templates目录）
- [ ] HTML链接：`../../*.html`（全局）或相对路径（同目录/agents目录）
- [ ] 模版页面保持通用和脱敏
- [ ] `templates.html` 中的预览链接指向 `templates/pages/`

---

## ⚠️ 注意事项

1. **不要混淆模版和实际应用**
   - 模版页面在 `templates/pages/`
   - 实际应用页面在 `agents/pages/`

2. **样式和脚本文件**
   - 模版目录有独立的CSS和JS文件
   - 可以复用agents目录的样式，但建议保持独立以便定制

3. **路径引用**
   - 从 `templates/pages/` 引用全局资源需要 `../../` 前缀
   - 从 `templates/pages/` 引用 `templates/` 下的资源需要 `../` 前缀

---

**最后更新**: 2024-12-09
