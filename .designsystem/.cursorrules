# Clouditera Design System - AI 助手规范

> 本文档用于指导 AI 助手在编写代码时遵循 Clouditera 设计系统规范。

## 🎯 核心原则

1. **必须使用 Design System 变量**：所有颜色、间距、字体等必须使用 design system 中定义的变量
2. **禁止硬编码**：禁止在代码中硬编码颜色值（如 `#298CFF`、`white`、`black` 等）
3. **语义化优先**：优先使用语义化的 token（如 `--clouditera-semantic-success`）而不是色板颜色
4. **保持一致性**：相同语义的场景必须使用相同的 token
5. **遵循设计偏好**：遵循设计风格偏好，保持视觉一致性

## 🎨 颜色使用规范

### 品牌色
- 主色：`var(--clouditera-brand-primary)` - 用于按钮、链接、强调元素
- 悬停：`var(--clouditera-brand-primary-hover)` - 用于 hover 状态
- 激活：`var(--clouditera-brand-primary-active)` - 用于 active 状态

### 语义色
- 成功：`var(--clouditera-semantic-success)` - 用于成功状态、成功提示
- 警告：`var(--clouditera-semantic-warning)` - 用于警告状态、警告提示
- 错误：`var(--clouditera-semantic-error)` - 用于错误状态、错误提示
- 信息：`var(--clouditera-semantic-info)` - 用于信息提示
- 链接：`var(--clouditera-semantic-link)` - 用于链接文本

### 文本颜色

#### 文本颜色优先级体系

根据信息重要性，页面文字元素应使用以下两种颜色进行区分：

**第一级 - 主要文本**：`var(--clouditera-neutral-text-primary)` (rgba(0, 0, 0, 0.87))
- **使用场景**：
  - 标题类：页面标题、卡片标题、模块标题、表格标题
  - 主要内容：任务名称、文件名称、消息文本、重要内容
  - 重要信息：表格内容、代码内容、主要操作文本
- **字重**：标题使用 `font-weight: 600`，内容使用 `font-weight: 400-500`

**第二级 - 次要文本**：`var(--clouditera-neutral-text-secondary)` (rgba(0, 0, 0, 0.65))
- **使用场景**：
  - 描述类：内容描述、说明文字、摘要文本
  - 辅助信息：时间、元数据、统计信息、占位符
  - 次要内容：思考文本、日志文本、辅助说明
- **字重**：通常使用 `font-weight: 400`

**禁用文本**：`var(--clouditera-neutral-text-disabled)` - 用于禁用状态

#### 文本颜色使用原则

1. **避免使用过多颜色**：页面文字仅使用 `text-primary` 和 `text-secondary` 两种颜色
2. **禁止使用灰度色板**：不要使用 `var(--clouditera-neutral-gray-6)`、`var(--clouditera-neutral-gray-7)`、`var(--clouditera-neutral-gray-9)` 等作为文本颜色
3. **语义化优先**：优先使用语义化的 `text-primary` 和 `text-secondary`，而不是具体的灰度值
4. **层次清晰**：通过颜色和字重（font-weight）共同建立视觉层次

### 背景颜色
- **主要背景**：`var(--clouditera-neutral-background-primary)` (#f5f5f5) - **用于页面整体布局背景**，避免页面过于灰暗
- **次要背景**：`var(--clouditera-neutral-background-secondary)` (#fafafa) - 用于次要区块、表格表头背景
- **三级背景**：`var(--clouditera-neutral-background-tertiary)` (#f0f0f0) - 用于分割线背景、进度条背景
- **白色背景**：`var(--clouditera-neutral-background-white)` (#ffffff) - **用于卡片、面板、对话框等主要内容背景**

**重要提示**：
- 页面整体背景必须使用 `var(--clouditera-neutral-background-primary)`，而不是白色
- 卡片和面板等容器背景使用 `var(--clouditera-neutral-background-white)`
- **禁止使用硬编码**：`white`、`#ffffff`、`#f5f5f5` 等

### 边框颜色
- 一级边框：`var(--clouditera-neutral-border-primary)` - 用于主要边框
- 二级边框：`var(--clouditera-neutral-border-secondary)` - 用于次要边框

### 白色文字
当需要在深色背景上显示白色文字时，使用：
```css
color: var(--clouditera-neutral-background-white);
```
**禁止使用**：`color: white;` 或 `color: #ffffff;`

## 📝 代码编写规范

### CSS 编写时

1. **检查颜色变量**：每次使用颜色时，必须使用 design system 变量
2. **检查硬编码**：禁止使用任何硬编码的颜色值
3. **使用语义化变量**：优先使用语义化的 token

### 示例

```css
/* ✅ 正确 - 页面整体背景 */
.page-container {
  background: var(--clouditera-neutral-background-primary);
}

/* ✅ 正确 - 卡片背景 */
.card {
  background: var(--clouditera-neutral-background-white);
  border: 1px solid var(--clouditera-neutral-border-secondary);
}

/* ✅ 正确 - 按钮 */
.button {
  background-color: var(--clouditera-brand-primary);
  color: var(--clouditera-neutral-background-white);
  border: 1px solid var(--clouditera-neutral-border-primary);
}

.button:hover {
  background-color: var(--clouditera-brand-primary-hover);
}

/* ❌ 错误 - 硬编码颜色 */
.page-container {
  background: #f5f5f5;  /* 应该使用 var(--clouditera-neutral-background-primary) */
}

.card {
  background: white;  /* 应该使用 var(--clouditera-neutral-background-white) */
}

.button {
  background-color: #298CFF;  /* 应该使用 var(--clouditera-brand-primary) */
  color: white;  /* 应该使用 var(--clouditera-neutral-background-white) */
  border: 1px solid #d9d9d9;  /* 应该使用 var(--clouditera-neutral-border-primary) */
}
```

## 🔍 代码审查检查点

在编写或修改 CSS 代码时，必须检查：

1. ✅ 所有颜色值都使用 design system 变量
2. ✅ 没有硬编码的颜色值（`#xxx`、`rgb()`、`rgba()`、`white`、`black`、`#ffffff`、`#f5f5f5` 等）
3. ✅ 页面整体背景使用 `var(--clouditera-neutral-background-primary)`
4. ✅ 卡片和面板背景使用 `var(--clouditera-neutral-background-white)`，而不是硬编码 `white`
5. ✅ 按钮使用品牌色变量
6. ✅ **文本颜色使用语义化变量**：
   - 主要文本使用 `var(--clouditera-neutral-text-primary)`
   - 次要文本使用 `var(--clouditera-neutral-text-secondary)`
   - **禁止使用** `var(--clouditera-neutral-gray-6)`、`var(--clouditera-neutral-gray-7)`、`var(--clouditera-neutral-gray-9)` 等作为文本颜色
7. ✅ 背景使用背景色变量
8. ✅ 状态色使用语义色变量
9. ✅ 边框使用边框色变量
10. ✅ 文字元素通过颜色和字重建立清晰的视觉层次

## 📚 参考文档

- **技术规范**：
  - 完整变量列表：`.designsystem/tokens.json`
  - 快速参考：`.designsystem/QUICK_REFERENCE.md`
  - 使用文档：`.designsystem/README.md`
  - CSS 变量文件：`frontend/css/tokens.css`
  - 间距设计规范：`.designsystem/SPACING.md` - 基于 Element Plus Space 组件的间距规范
- **详细参考**（可选）：
  - 颜色设计规范：`.designsystem/.color` - 详细的颜色体系说明

## ⚠️ 特殊情况

### 渐变和阴影

渐变和阴影中的颜色可以使用 rgba 格式，但应基于 design system 的颜色值：

```css
/* ✅ 正确 - 基于品牌色的渐变 */
.gradient {
  background: linear-gradient(
    135deg,
    var(--clouditera-brand-primary) 0%,
    var(--clouditera-brand-primary-active) 100%
  );
}

/* ✅ 正确 - 基于品牌色的阴影 */
.shadow {
  box-shadow: 0 4px 12px rgba(41, 140, 255, 0.25);
  /* 41, 140, 255 对应 #298CFF (brand-primary) */
}
```

### 装饰性渐变

某些装饰性渐变（如 hero section 的紫色渐变）可以保留，但应尽量使用 design system 中的颜色。

## 🎨 设计风格偏好

### 颜色使用偏好

#### 背景色使用原则

- **页面整体背景**：必须使用 `var(--clouditera-neutral-background-primary)` (#f5f5f5) 作为页面布局背景，避免页面过于灰暗
- **卡片和面板背景**：使用 `var(--clouditera-neutral-background-white)` (#ffffff) 作为卡片、面板、对话框等内容的背景
- **区块背景**：使用 `var(--clouditera-neutral-background-secondary)` (#fafafa) 作为次要区块的背景
- **禁止硬编码白色**：必须使用 `var(--clouditera-neutral-background-white)` 而不是 `white` 或 `#ffffff`

#### 装饰性颜色使用

- **减少蓝色线条装饰**：避免使用蓝色分割线、边框线条等装饰，这些会显得突兀
- **避免大面积蓝色色块**：不要使用过多的蓝色大面积背景色块
- **适度使用淡色背景**：可以使用低透明度（10%左右）的蓝色背景作为色块装饰
- **标签与按钮区分**：
  - 按钮：实色背景 + 白色文字 + 阴影
  - 标签：淡色背景（10%透明度）+ 彩色文字 + 无阴影

#### 颜色变量使用优先级

1. **优先使用语义化变量**：如 `--clouditera-semantic-success` 而不是 `--clouditera-palette-green-5`
2. **背景色变量**：优先使用 `--clouditera-neutral-background-*` 系列
3. **文本色变量**：优先使用 `--clouditera-neutral-text-*` 系列
4. **边框色变量**：优先使用 `--clouditera-neutral-border-*` 系列
5. **色板颜色**：仅在需要特定色板颜色时使用 `--clouditera-palette-{color}-{level}`

### 装饰元素偏好

- **少用线条装饰**：避免使用分割线、边框线条等
- **多用背景装饰**：
  - 使用径向渐变（radial-gradient）增加视觉层次
  - 使用装饰圆形（positioned circles）增加充实感
  - 使用多层背景渐变叠加
- **避免空洞设计**：卡片和模块要有足够的视觉内容，避免显得空洞

### 文字和布局偏好

- **文字层次清晰**：
  - 重要文字：大字号（32px+）、加粗（700）、深色
  - 次要文字：可以弱化或去除标题
  - 标签文字：适中字号（14px）、中等字重（600）
- **布局要有优先级**：通过字体大小、颜色、间距建立清晰的视觉层次

### 卡片和模块设计偏好

- **卡片要充实**：使用背景装饰、多层渐变来增加视觉充实感
- **模块区分**：不同模块使用不同的背景色、装饰样式来区分
- **适度使用色块**：可以使用色块，但要控制透明度和面积

### 整体风格偏好

- **简洁现代**：避免过于花哨的装饰
- **专业可信**：体现产品的专业性和技术感
- **视觉层次清晰**：通过大小、颜色、间距建立清晰的层次关系

## 📐 间距使用规范

### 间距体系

**基准单位：8px**

所有间距值都基于 8px 的倍数，确保视觉一致性和对齐。

### Clouditera 内置间距尺寸

| 尺寸名称 | 值 | 说明 | 使用场景 |
|---------|-----|------|----------|
| **small** | 8px | 小间距（默认） | 紧密排列的元素、标签间距、图标间距 |
| **default** | 12px | 标准间距 | 输入框内边距、小按钮内边距、元素组间距 |
| **large** | 16px | 大间距 | 卡片内边距、按钮内边距、标准元素间距 |

**默认间距大小**：`small` (8px)

### 使用统一的间距管理

```css
/* ✅ 推荐：使用 gap 属性统一管理间距 */
.button-group {
  display: flex;
  gap: 8px;  /* small - 默认 */
}

/* 指定间距大小 */
.button-group-large {
  gap: 16px;  /* large */
}

/* 自定义间距值（必须是 8px 的倍数） */
.button-group-custom {
  gap: 24px;  /* 3× */
}
```

### CSS 间距使用

#### 内边距（Padding）

```css
/* 使用 Element Plus 内置尺寸 */
.padding-small { padding: 8px; }      /* small */
.padding-default { padding: 12px; }   /* default */
.padding-large { padding: 16px; }      /* large */

/* 按钮内边距 */
.btn { padding: 10px 20px; }           /* 垂直：md，水平：lg */
.btn-large { padding: 14px 32px; }    /* 垂直：base，水平：2xl */

/* 输入框内边距 - 对应 default (12px) */
.input { padding: 12px 16px; }        /* default × large */

/* 卡片内边距 */
.card { padding: 16px; }               /* large */
.card-large { padding: 24px; }        /* 3× */
```

#### 外边距（Margin）和间距（Gap）

```css
/* 使用 Element Plus 内置尺寸 */
.margin-small { margin: 8px; }        /* small */
.margin-default { margin: 12px; }     /* default */
.margin-large { margin: 16px; }       /* large */

/* Flexbox/Grid 间距 */
.flex-container {
  gap: 8px;   /* small - 默认 */
  gap: 12px;  /* default */
  gap: 16px;  /* large */
}
```

### 间距使用原则

1. **使用统一的间距管理**：使用 `gap` 属性管理组件之间的间距
2. **使用内置尺寸**：优先使用 `small` (8px)、`default` (12px)、`large` (16px)
3. **自定义值必须是 8px 的倍数**：如 20px, 24px, 32px, 40px, 48px
4. **禁止使用非 8px 倍数的值**：如 5px, 7px, 9px, 11px, 13px, 15px, 17px, 19px, 21px, 23px, 25px
5. **保持一致性**：相同类型的元素使用相同的间距

### 间距检查清单

在编写 CSS 代码时，检查：
- [ ] 使用 `gap` 属性统一管理组件间距
- [ ] 所有间距值都是 8px 的倍数
- [ ] 优先使用内置尺寸（small: 8px、default: 12px、large: 16px）
- [ ] 相同类型的元素使用相同的间距
- [ ] 没有使用非 8px 倍数的间距值

## 🎯 执行要求

1. **每次编写 CSS 代码时**，必须参考本规范
2. **每次修改颜色时**，必须检查是否使用了 design system 变量
3. **发现硬编码颜色时**，必须立即替换为 design system 变量
4. **不确定使用哪个变量时**，参考 `QUICK_REFERENCE.md` 或 `tokens.json`

