# 任务异常终止 - UI 设计文档

> **基于 Clouditera 设计系统**
> **设计时间**: 2025-12-24
> **设计版本**: v1.0.0

---

## 📋 目录

1. [设计概述](#设计概述)
2. [设计原则](#设计原则)
3. [组件设计](#组件设计)
4. [颜色系统](#颜色系统)
5. [布局规范](#布局规范)
6. [交互设计](#交互设计)
7. [代码实现](#代码实现)
8. [使用指南](#使用指南)

---

## 设计概述

### 设计目标

为自规划任务执行助手设计一套完整的异常状态展示系统，确保：

1. **清晰传达异常信息**：用户能快速识别任务失败的原因和影响范围
2. **保留已完成工作**：展示任务执行到异常前的所有成果
3. **提供恢复路径**：引导用户采取后续行动（重新开始、调整参数等）
4. **统一视觉语言**：所有异常状态使用一致的颜色、图标和样式

### 设计范围

- ✅ 任务列表中的异常标记
- ✅ 任务头部异常状态徽章
- ✅ 异常阶段卡片设计
- ✅ 系统异常提示卡片
- ✅ 部分完成结果展示
- ✅ 重新开始操作按钮
- ✅ 右侧工作区异常状态

---

## 设计原则

### 1. 遵循设计系统 ⭐

**所有颜色使用 CSS Variables (Design Tokens)，禁止硬编码**

```css
/* ✅ 正确做法 */
.error-alert {
    background: var(--clouditera-palette-red-0);
    border-color: var(--clouditera-palette-red-4);
}

/* ❌ 错误做法 */
.error-alert {
    background: #fee2e2;
    border-color: #dc2626;
}
```

### 2. 视觉层级清晰 📐

通过颜色、大小、间距建立信息优先级：

| 优先级 | 元素 | 视觉特征 |
|--------|------|----------|
| P0 - 最高 | 系统异常提示 | 红色背景 + 红色边框 + 警告图标 |
| P1 - 高 | 异常阶段卡片 | 红色左边框 + 浅红背景 |
| P2 - 中 | 异常任务项 | 红色图标 + 浅红背景 |
| P3 - 低 | 建议提示 | 黄色背景 + 灯泡图标 |

### 3. 语义化颜色 🎨

使用统一的颜色语义：

| 状态 | 主色 | 背景色 | 边框色 | 用途 |
|------|------|--------|--------|------|
| **错误/异常** | `palette-red-6` | `palette-red-0` | `palette-red-4` | 系统异常、任务失败 |
| **警告/建议** | `palette-gold-7` | `palette-gold-0` | `palette-gold-4` | 操作建议、注意事项 |
| **成功** | `palette-green-6` | `palette-green-0` | `palette-green-2` | 已完成阶段、成功操作 |
| **进行中** | `palette-blue-6` | `palette-blue-0` | `palette-blue-2` | 运行中的任务 |

### 4. 渐进式披露 📊

信息按重要程度分层展示：

1. **第一层（立即可见）**：任务失败状态、异常类型
2. **第二层（对话区）**：异常发生的阶段、已完成的工作
3. **第三层（右侧面板）**：详细日志、文件列表

---

## 组件设计

### 1. 状态徽章（Status Badge）

**用途**：在任务头部标识系统异常状态

#### 设计规范

| 属性 | 值 |
|------|-----|
| 内边距 | `4px 12px` |
| 圆角 | `4px` |
| 字号 | `12px` |
| 字重 | `600` (Semi-bold) |
| 行高 | `1.5` |

#### 颜色方案

```css
/* 系统异常徽章 */
.status-badge.error {
    background: var(--clouditera-palette-red-0);
    color: var(--clouditera-palette-red-6);
    border: 1px solid var(--clouditera-palette-red-2);
}
```

#### 视觉示例

```
┌──────────────────┐
│  系统异常        │  ← 浅红背景 + 深红文字
└──────────────────┘
```

---

### 2. 系统异常提示卡片（Error Alert）

**用途**：在异常阶段内显示详细的错误信息

#### 设计规范

| 属性 | 值 |
|------|-----|
| 背景色 | `palette-red-0` |
| 边框 | `1px solid palette-red-4` |
| 圆角 | `8px` |
| 内边距 | `16px` |
| 外边距 | `12px 0 0 0` |

#### 结构设计

```
┌─────────────────────────────────────────┐
│ ⚠️  系统异常                            │ ← 头部
├─────────────────────────────────────────┤
│ 错误类型: OutOfMemoryError              │ ← 内容
│ 错误信息: 分析大型代码文件时内存...     │
└─────────────────────────────────────────┘
```

#### 代码模板

```html
<div class="error-alert">
    <div class="error-alert-header">
        <span class="error-alert-icon">⚠️</span>
        <span class="error-alert-title">系统异常</span>
    </div>
    <div class="error-alert-content">
        <p>错误类型: OutOfMemoryError</p>
        <p>错误信息: 分析大型代码文件时内存资源不足</p>
    </div>
</div>
```

---

### 3. 异常阶段卡片（Error Phase Card）

**用途**：展示发生异常的执行阶段及其上下文

#### 设计规范

| 属性 | 值 |
|------|-----|
| 背景色 | `palette-red-0` |
| 边框 | `1px solid palette-red-2` |
| 左边框 | `3px solid palette-red-5` (强调) |
| 圆角 | `8px` |
| 内边距 | `16px` |

#### 阶段徽章规范

```css
.phase-badge.error {
    background: var(--clouditera-palette-red-5);
    color: var(--clouditera-neutral-background-white);
    padding: 4px 10px;
    border-radius: 4px;
    font-size: 12px;
    font-weight: 600;
}
```

#### 视觉示例

```
┃ ┌─────────────────────────────────────┐
┃ │ [阶段 2]  威胁建模与漏洞分析  ❌ 异常终止 │
┃ ├─────────────────────────────────────┤
┃ │ 💭 开始分析...                       │
┃ │ 🔧 读取文件                          │
┃ │ ⚠️ 系统异常: OutOfMemoryError       │
┃ └─────────────────────────────────────┘
↑ 3px 红色左边框
```

---

### 4. 漏洞发现卡片（Vulnerability Finding）

**用途**：展示在异常前发现的安全漏洞

#### 设计规范

| 属性 | 值 |
|------|-----|
| 背景色 | `neutral-background-white` |
| 边框 | `1px solid neutral-border-secondary` |
| 圆角 | `8px` |
| 内边距 | `12px` |

#### 危险等级徽章

| 等级 | 背景色 | 文字色 | 边框色 |
|------|--------|--------|--------|
| 高危 | `palette-red-0` | `palette-red-6` | `palette-red-3` |
| 中危 | `palette-gold-0` | `palette-gold-7` | `palette-gold-3` |
| 低危 | `palette-blue-0` | `palette-blue-6` | `palette-blue-2` |

#### 代码模板

```html
<div class="vulnerability-finding">
    <div class="vuln-header">
        <span class="vuln-severity high">高危</span>
        <span class="vuln-title">SQL注入漏洞</span>
    </div>
    <div class="vuln-location">UserRepository.java:67</div>
    <div class="vuln-description">用户输入未经过滤直接拼接到SQL语句中</div>
</div>
```

---

### 5. 警告提示卡片（Warning Alert）

**用途**：提供操作建议和后续步骤

#### 设计规范

| 属性 | 值 |
|------|-----|
| 背景色 | `palette-gold-0` |
| 边框 | `1px solid palette-gold-4` |
| 圆角 | `6px` |
| 内边距 | `12px` |

#### 代码模板

```html
<div class="warning-alert">
    <div class="warning-alert-content">
        💡 <strong>建议:</strong> 您可以点击"重新开始"按钮重新执行此任务
    </div>
</div>
```

---

### 6. 重新开始按钮（Restart Button）

**用途**：提供快速重启任务的操作入口

#### 设计规范

| 属性 | 值 |
|------|-----|
| 背景色 | `brand-primary` |
| 文字色 | `neutral-background-white` |
| 内边距 | `6px 16px` |
| 圆角 | `6px` |
| 字号 | `13px` |
| 字重 | `500` (Medium) |

#### 交互状态

```css
.btn-restart {
    background: var(--clouditera-brand-primary);
    transition: all 0.2s;
}

.btn-restart:hover {
    background: var(--clouditera-brand-primary-hover);
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(41, 140, 255, 0.2);
}

.btn-restart:active {
    background: var(--clouditera-brand-primary-active);
    transform: translateY(0);
}
```

---

## 颜色系统

### 主要颜色 Token

```css
/* 错误/异常状态 */
--clouditera-palette-red-0: #fff1f0;    /* 最浅背景 */
--clouditera-palette-red-2: #ffa39e;    /* 浅边框 */
--clouditera-palette-red-4: #ff4d4f;    /* 强调边框 */
--clouditera-palette-red-5: #f5222d;    /* 主色 */
--clouditera-palette-red-6: #cf1322;    /* 文字色 */
--clouditera-palette-red-8: #820014;    /* 深色文字 */

/* 警告/建议状态 */
--clouditera-palette-gold-0: #fffbe6;   /* 最浅背景 */
--clouditera-palette-gold-4: #ffc53d;   /* 边框 */
--clouditera-palette-gold-7: #ad6800;   /* 文字色 */
--clouditera-palette-gold-9: #613400;   /* 深色文字 */

/* 成功状态 */
--clouditera-palette-green-0: #f6ffed;  /* 背景 */
--clouditera-palette-green-2: #b7eb8f;  /* 边框 */
--clouditera-palette-green-6: #389e0d;  /* 文字色 */
```

### 颜色使用场景

| 场景 | Token | 示例 |
|------|-------|------|
| 异常徽章背景 | `palette-red-0` | 系统异常 |
| 异常徽章文字 | `palette-red-6` | 系统异常 |
| 异常卡片边框 | `palette-red-4` | 阶段卡片 |
| 异常卡片强调边框 | `palette-red-5` | 阶段卡片左边框 |
| 异常文字 | `palette-red-8` | 错误详情 |
| 建议卡片背景 | `palette-gold-0` | 操作建议 |
| 建议卡片文字 | `palette-gold-9` | 建议内容 |

---

## 布局规范

### 间距系统（基于 8px）

```css
/* 组件内边距 */
--spacing-xs: 4px;    /* 徽章内边距 */
--spacing-sm: 8px;    /* 小间距 */
--spacing-md: 12px;   /* 中间距 */
--spacing-lg: 16px;   /* 大间距 */
--spacing-xl: 20px;   /* 超大间距 */

/* 组件间距 */
--gap-xs: 6px;        /* 图标与文字间距 */
--gap-sm: 8px;        /* 小组件间距 */
--gap-md: 12px;       /* 中等组件间距 */
--gap-lg: 16px;       /* 大组件间距 */
```

### 圆角规范

```css
--radius-sm: 4px;     /* 徽章 */
--radius-md: 6px;     /* 按钮、小卡片 */
--radius-lg: 8px;     /* 大卡片 */
```

### 响应式断点

```css
/* 移动端 */
@media (max-width: 768px) {
    .error-alert {
        padding: 12px;  /* 减小内边距 */
    }

    .error-alert-content {
        font-size: 12px;  /* 缩小字号 */
    }
}
```

---

## 交互设计

### 1. 悬停效果（Hover）

#### 重新开始按钮

```css
.btn-restart:hover {
    background: var(--clouditera-brand-primary-hover);
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(41, 140, 255, 0.2);
}
```

**效果**：按钮向上浮起 1px，显示阴影

#### 失败任务项

```css
.recent-task-item.active.failed:hover {
    background: var(--clouditera-palette-red-1);
}
```

**效果**：背景色变深

### 2. 激活效果（Active）

```css
.btn-restart:active {
    background: var(--clouditera-brand-primary-active);
    transform: translateY(0);
}
```

**效果**：按钮恢复原位，颜色变深

### 3. 过渡动画

```css
.btn-restart {
    transition: all 0.2s;
}
```

**时长**：200ms
**缓动函数**：ease（默认）

---

## 代码实现

### 文件结构

```
frontend/
├── templates/
│   ├── pages/
│   │   └── template-planning-error.html    # 异常页面
│   └── css/
│       ├── template-planning.css           # 基础样式
│       └── template-planning-error-states.css  # 异常状态样式 ✨
└── css/
    └── tokens.css                          # 设计 Token
```

### 样式引入顺序

```html
<head>
    <!-- 1. 设计 Token（最先加载） -->
    <link rel="stylesheet" href="../../css/tokens.css">

    <!-- 2. 基础样式 -->
    <link rel="stylesheet" href="../../css/common.css">
    <link rel="stylesheet" href="../../css/dashboard.css">

    <!-- 3. 页面样式 -->
    <link rel="stylesheet" href="../css/template-detail.css">
    <link rel="stylesheet" href="../css/template-planning.css">

    <!-- 4. 异常状态样式（覆盖基础样式） -->
    <link rel="stylesheet" href="../css/template-planning-error-states.css">

    <!-- 5. 组件样式 -->
    <link rel="stylesheet" href="../../components/agent-sidebar.css">
    <link rel="stylesheet" href="../../components/chat-input.css">
</head>
```

### 核心 CSS 类名

| 类名 | 用途 | 状态修饰符 |
|------|------|-----------|
| `.status-badge` | 状态徽章 | `.error`, `.success`, `.running` |
| `.error-alert` | 系统异常提示 | - |
| `.warning-alert` | 警告提示 | - |
| `.phase-card` | 阶段卡片 | `.error` |
| `.phase-badge` | 阶段徽章 | `.error` |
| `.vulnerability-finding` | 漏洞发现 | - |
| `.vuln-severity` | 漏洞等级 | `.high`, `.medium`, `.low` |
| `.btn-restart` | 重新开始按钮 | - |
| `.task-list-item` | 任务清单项 | `.error`, `.completed` |
| `.phase-progress-item` | 阶段进度项 | `.failed`, `.completed` |

---

## 使用指南

### 1. 创建系统异常提示

```html
<div class="error-alert">
    <div class="error-alert-header">
        <span class="error-alert-icon">⚠️</span>
        <span class="error-alert-title">系统异常</span>
    </div>
    <div class="error-alert-content">
        <p>错误类型: TimeoutError</p>
        <p>错误信息: 任务执行超时</p>
    </div>
</div>
```

### 2. 创建异常阶段卡片

```html
<div class="phase-card error">
    <div class="phase-header">
        <span class="phase-badge error">阶段 2</span>
        <span class="phase-title">数据处理</span>
        <span class="phase-status error">❌ 异常终止</span>
    </div>
    <div class="phase-content">
        <!-- 思考、工具调用等内容 -->
        <div class="error-alert">
            <!-- 异常提示 -->
        </div>
    </div>
</div>
```

### 3. 添加重新开始按钮

```html
<button class="btn-restart">
    <span>🔄</span>
    <span>重新开始</span>
</button>
```

### 4. 展示漏洞发现

```html
<div class="vulnerability-finding">
    <div class="vuln-header">
        <span class="vuln-severity high">高危</span>
        <span class="vuln-title">XSS跨站脚本</span>
    </div>
    <div class="vuln-location">user-profile.html:23</div>
    <div class="vuln-description">用户昵称未进行HTML转义</div>
</div>
```

### 5. 任务列表中标记失败任务

```html
<a href="template-planning-error.html" class="recent-task-item active failed">
    <span class="task-status-icon failed">❌</span>
    <div class="task-name">源代码漏洞挖掘任务</div>
</a>
```

---

## 可访问性（A11y）

### 颜色对比度

所有文字与背景的对比度均 ≥ 4.5:1，符合 WCAG AA 标准：

| 组合 | 对比度 | 等级 |
|------|--------|------|
| `palette-red-6` on `palette-red-0` | 5.2:1 | ✅ AA |
| `palette-gold-9` on `palette-gold-0` | 7.8:1 | ✅ AAA |
| `white` on `palette-red-5` | 6.1:1 | ✅ AA |

### 语义化标签

```html
<!-- ✅ 使用语义化标签 -->
<section class="error-alert" role="alert">
    <h4 class="error-alert-title">系统异常</h4>
    <!-- ... -->
</section>

<!-- ❌ 避免纯 div 堆砌 -->
<div class="error-alert">
    <div class="error-alert-title">系统异常</div>
    <!-- ... -->
</div>
```

### 键盘操作

所有交互元素支持键盘操作：

```html
<button class="btn-restart"
        tabindex="0"
        aria-label="重新开始任务">
    <span>🔄</span>
    <span>重新开始</span>
</button>
```

---

## 设计决策记录

### 为什么使用左边框强调异常阶段？

**原因**：
1. 左边框是视觉焦点的自然起点（从左到右阅读习惯）
2. 3px 粗边框提供足够的视觉权重
3. 不遮挡内容，保持信息完整性

### 为什么选择红色系作为异常主色？

**原因**：
1. 红色是通用的错误/危险色彩语言
2. Clouditera 设计系统提供完整的红色色阶
3. 红色能引起注意但不会过于刺眼（使用浅色背景 + 深色文字）

### 为什么保留已完成的工作成果？

**原因**：
1. 提升用户体验：让用户看到智能体已完成的工作价值
2. 调试友好：帮助开发者定位问题发生的准确位置
3. 可恢复性：用户可以基于部分成果继续工作

---

## 后续优化方向

### 短期（v1.1）

- [ ] 添加异常详情可展开/折叠功能
- [ ] 提供异常日志导出功能
- [ ] 优化移动端布局

### 中期（v1.2）

- [ ] 支持自定义异常类型和样式
- [ ] 添加异常统计和趋势分析
- [ ] 实现异常自动重试机制

### 长期（v2.0）

- [ ] 智能异常诊断建议
- [ ] 异常恢复检查点系统
- [ ] 多任务异常对比视图

---

## 相关资源

### 设计系统文档
- [颜色系统](./../../../.claude/skills/designsystem/colors.md)
- [组件规范](./../../../.claude/skills/designsystem/components.md)
- [排版系统](./../../../.claude/skills/designsystem/typography.md)

### 实现文件
- HTML: `frontend/templates/pages/template-planning-error.html`
- CSS: `frontend/templates/css/template-planning-error-states.css`
- 功能说明: `产品设计文档/功能说明/任务异常终止-界面功能说明.md`

---

**文档版本**: v1.0.0
**最后更新**: 2025-12-24
**维护者**: Clouditera 设计团队
